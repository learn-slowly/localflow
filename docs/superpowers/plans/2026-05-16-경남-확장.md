# 경남 전역 확장 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 루트 `/`를 경남 22개 시군구 지도로 바꾸고, 진주 분석은 `/jinju`로 옮긴다. 다른 시군구는 인구 레이어만, 진주만 전체 레이어 활성.

**Architecture:** `/[city]` 동적 라우트로 도시별 화면 통일. `MapContainer`에 `cityCode` prop을 추가해 진주 외 도시에선 진주 전용 데이터 import를 비활성. 경남 전역용 `GyeongnamMap` 컴포넌트를 신규로 만들어 `/`에서 사용.

**Tech Stack:** Next.js 16 App Router, TypeScript, 카카오맵 SDK, Python 3 (빌드 스크립트), Tailwind CSS.

**스펙:** `docs/superpowers/specs/2026-05-16-경남-확장-design.md`

---

## File Structure

### 생성
- `scripts/build-gyeongnam-summary.py` — 22개 시군구 인구 요약 빌드
- `scripts/build-gyeongnam-boundary.py` — 22개 시군구 경계 GeoJSON 빌드
- `src/data/gyeongnam-cities.json` — 시군구별 인구 요약 (빌드 산출물)
- `public/data/gyeongnam-boundary.json` — 시군구 단위 GeoJSON (빌드 산출물)
- `src/app/[city]/page.tsx` — 도시별 메인 (지도 + 사이드 패널)
- `src/app/[city]/district/[id]/page.tsx` — 선거구 상세
- `src/components/Map/GyeongnamMap.tsx` — 경남 전역 지도 컴포넌트
- `src/lib/cityData.ts` — 도시별 데이터 가용성 헬퍼

### 수정
- `src/app/page.tsx` — 진주 브리핑 → 경남 지도로 교체
- `src/components/Map/MapContainer.tsx` — `cityCode` prop 추가, 진주 외 도시에서 진주 데이터 import 비활성

### 삭제
- `src/app/map/page.tsx` — `/[city]`로 흡수
- `src/app/district/[id]/page.tsx` — `/[city]/district/[id]`로 이동

---

## Task 0: 미커밋 사항 정리·커밋

작업 시작 전에 현재 미커밋 변경을 보존한다. 이번 작업에서 이 파일들을 이동·수정할 거라서 안전망이 필요하다.

**Files:**
- Stage: `src/app/page.tsx`, `src/app/globals.css`, `src/app/district/[id]/page.tsx`, `src/app/map/page.tsx`, `src/lib/district-recommender.ts`

- [ ] **Step 1: 현재 변경분 확인**

```bash
git status -uno
git diff --stat
```

- [ ] **Step 2: .DS_Store 무시 처리**

`.gitignore`에 `.DS_Store` 줄이 있는지 확인:

```bash
grep -q "^\.DS_Store$" .gitignore || echo ".DS_Store" >> .gitignore
```

- [ ] **Step 3: 미커밋 작업물 커밋**

```bash
git add src/app/page.tsx src/app/globals.css src/app/district src/app/map src/lib/district-recommender.ts .gitignore
git commit -m "$(cat <<'EOF'
경남 확장 직전 작업물 스냅샷

진주 선거구 브리핑/지도/상세 페이지 미커밋 변경분을 정리해 보존.
다음 커밋부터 /[city] 동적 라우트로 이동 예정.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: 클린 상태 확인**

```bash
git status
```

Expected: `nothing to commit, working tree clean` (.DS_Store 같은 추적 안 되는 파일 외)

---

## Task 1: 경남 시군구 인구 요약 빌드 스크립트

빌드타임에 `nationwide-population.json` (10자리 법정동 코드별 인구)을 22개 시군구별로 합산해 `src/data/gyeongnam-cities.json`을 만든다.

**Files:**
- Create: `scripts/build-gyeongnam-summary.py`
- Output: `src/data/gyeongnam-cities.json`

- [ ] **Step 1: 스크립트 작성**

```python
#!/usr/bin/env python3
"""경남 22개 시군구 인구 요약 빌드.

src/config/cities/index.ts의 cities 키를 따라
public/data/nationwide-population.json에서 시군구별 인구를 합산해
src/data/gyeongnam-cities.json을 생성한다.
"""

import json
import re
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
NATIONWIDE = ROOT / "public/data/nationwide-population.json"
OUT = ROOT / "src/data/gyeongnam-cities.json"

# 경남 22개 시군구 (행정표준코드 5자리 → 도시 키, 한글명, 지도 중심)
GYEONGNAM = [
    ("48121", "changwon-uichang",      "창원시의창구",   [35.2994, 128.6546]),
    ("48123", "changwon-seongsan",     "창원시성산구",   [35.2018, 128.6751]),
    ("48125", "changwon-masanhappo",   "창원시마산합포구", [35.1370, 128.5084]),
    ("48127", "changwon-masanhoewon",  "창원시마산회원구", [35.2354, 128.5586]),
    ("48129", "changwon-jinhae",       "창원시진해구",   [35.1243, 128.7185]),
    ("48170", "jinju",                 "진주시",        [35.1798, 128.1076]),
    ("48220", "tongyeong",             "통영시",        [34.7925, 128.3828]),
    ("48240", "sacheon",               "사천시",        [35.0335, 128.0460]),
    ("48250", "gimhae",                "김해시",        [35.2642, 128.8448]),
    ("48270", "miryang",               "밀양시",        [35.4890, 128.7744]),
    ("48310", "geoje",                 "거제시",        [34.8731, 128.6132]),
    ("48330", "yangsan",               "양산시",        [35.3957, 129.0527]),
    ("48720", "uiryeong",              "의령군",        [35.3903, 128.2745]),
    ("48730", "haman",                 "함안군",        [35.2914, 128.4374]),
    ("48740", "changnyeong",           "창녕군",        [35.4998, 128.4969]),
    ("48820", "goseong",               "고성군",        [35.0169, 128.2773]),
    ("48840", "namhae",                "남해군",        [34.8053, 127.9534]),
    ("48850", "hadong",                "하동군",        [35.1006, 127.8007]),
    ("48860", "sancheong",             "산청군",        [35.3846, 127.9092]),
    ("48870", "hamyang",               "함양군",        [35.5622, 127.7277]),
    ("48880", "geochang",              "거창군",        [35.7169, 127.9054]),
    ("48890", "hapcheon",              "합천군",        [35.5648, 128.1596]),
]


def main():
    with NATIONWIDE.open(encoding="utf-8") as f:
        pop = json.load(f)

    result = {}
    for code, key, name, center in GYEONGNAM:
        total = households = male = female = 0
        for dong_code, rec in pop.items():
            if not dong_code.startswith(code):
                continue
            total      += rec.get("p", 0)
            households += rec.get("h", 0)
            male       += rec.get("m", 0)
            female     += rec.get("f", 0)
        result[key] = {
            "code": code,
            "name": name,
            "totalPopulation": total,
            "households": households,
            "male": male,
            "female": female,
            "center": center,
        }
        print(f"{key:25s} 인구 {total:>10,}  세대 {households:>8,}")

    OUT.parent.mkdir(parents=True, exist_ok=True)
    with OUT.open("w", encoding="utf-8") as f:
        json.dump(result, f, ensure_ascii=False, indent=2)
    print(f"\n→ {OUT}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실행 권한·실행**

```bash
chmod +x scripts/build-gyeongnam-summary.py
python3 scripts/build-gyeongnam-summary.py
```

Expected: 22줄 출력 (시군구별 인구·세대 수), 마지막에 `→ /Users/.../src/data/gyeongnam-cities.json`.

- [ ] **Step 3: 산출물 검증**

```bash
python3 -c "import json; d = json.load(open('src/data/gyeongnam-cities.json')); print(len(d), 'cities'); print('jinju:', d['jinju'])"
```

Expected: `22 cities` + 진주 인구가 30만 명 이상 합리적 범위.

- [ ] **Step 4: 커밋**

```bash
git add scripts/build-gyeongnam-summary.py src/data/gyeongnam-cities.json
git commit -m "$(cat <<'EOF'
경남 22개 시군구 인구 요약 빌드 스크립트 추가

nationwide-population.json에서 시군구별 인구·세대를 합산해
src/data/gyeongnam-cities.json 생성. 경남 전역 지도 인구 코로플레스
색칠에 사용.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 경남 시군구 경계 GeoJSON 빌드 스크립트

`nationwide-boundary.json` (전국 행정동 GeoJSON)에서 경남만 추출해 시군구 단위로 묶은 GeoJSON을 만든다. dissolve 없이 sgg 코드별로 동 폴리곤을 MultiPolygon으로 합친다(shapely 의존성 회피).

**Files:**
- Create: `scripts/build-gyeongnam-boundary.py`
- Output: `public/data/gyeongnam-boundary.json`

- [ ] **Step 1: 스크립트 작성**

```python
#!/usr/bin/env python3
"""경남 22개 시군구 경계 GeoJSON 빌드.

nationwide-boundary.json (전국 행정동 GeoJSON)에서 경남(sido=48)을
추출하고, sgg 코드별로 동 폴리곤을 MultiPolygon으로 합쳐 시군구 단위
22개 Feature를 만든다. dissolve는 외부 라이브러리 없이 좌표 리스트
연결만 수행한다.
"""

import json
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
SRC = ROOT / "public/data/nationwide-boundary.json"
OUT = ROOT / "public/data/gyeongnam-boundary.json"

CITY_KEY = {
    "48121": "changwon-uichang",
    "48123": "changwon-seongsan",
    "48125": "changwon-masanhappo",
    "48127": "changwon-masanhoewon",
    "48129": "changwon-jinhae",
    "48170": "jinju",
    "48220": "tongyeong",
    "48240": "sacheon",
    "48250": "gimhae",
    "48270": "miryang",
    "48310": "geoje",
    "48330": "yangsan",
    "48720": "uiryeong",
    "48730": "haman",
    "48740": "changnyeong",
    "48820": "goseong",
    "48840": "namhae",
    "48850": "hadong",
    "48860": "sancheong",
    "48870": "hamyang",
    "48880": "geochang",
    "48890": "hapcheon",
}


def main():
    with SRC.open(encoding="utf-8") as f:
        data = json.load(f)

    # sgg → {sggnm, polygons[]}
    grouped: dict[str, dict] = {}
    for feat in data["features"]:
        props = feat["properties"]
        sgg = props.get("sgg")
        if sgg not in CITY_KEY:
            continue
        bucket = grouped.setdefault(sgg, {
            "sgg": sgg,
            "sggnm": props.get("sggnm"),
            "polygons": [],
        })
        geom = feat["geometry"]
        if geom["type"] == "Polygon":
            bucket["polygons"].append(geom["coordinates"])
        elif geom["type"] == "MultiPolygon":
            bucket["polygons"].extend(geom["coordinates"])

    features = []
    for sgg, b in sorted(grouped.items()):
        features.append({
            "type": "Feature",
            "properties": {
                "code": sgg,
                "key": CITY_KEY[sgg],
                "name": b["sggnm"],
            },
            "geometry": {
                "type": "MultiPolygon",
                "coordinates": b["polygons"],
            },
        })
        print(f"{CITY_KEY[sgg]:25s} sgg={sgg}  polygons={len(b['polygons'])}")

    out = {"type": "FeatureCollection", "features": features}
    with OUT.open("w", encoding="utf-8") as f:
        json.dump(out, f, ensure_ascii=False)
    size_mb = OUT.stat().st_size / 1024 / 1024
    print(f"\n→ {OUT}  ({size_mb:.2f} MB)")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 실행**

```bash
chmod +x scripts/build-gyeongnam-boundary.py
python3 scripts/build-gyeongnam-boundary.py
```

Expected: 22줄 출력 (시군구별 폴리곤 수), 출력 파일 크기 ~1MB 내외.

- [ ] **Step 3: 산출물 검증**

```bash
python3 -c "import json; d=json.load(open('public/data/gyeongnam-boundary.json')); print(len(d['features']), 'features'); print(d['features'][0]['properties'])"
```

Expected: `22 features` + 첫 feature properties에 `code`, `key`, `name`.

- [ ] **Step 4: 커밋**

```bash
git add scripts/build-gyeongnam-boundary.py public/data/gyeongnam-boundary.json
git commit -m "$(cat <<'EOF'
경남 시군구 경계 GeoJSON 빌드 스크립트 추가

nationwide-boundary.json에서 경남 동 폴리곤을 시군구 단위 22개
Feature로 묶음. dissolve 없이 좌표 그룹핑만 수행해 외부 의존성 회피.
public/data/gyeongnam-boundary.json 생성.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: `MapContainer`에 `cityCode` prop 추가

`MapContainer`는 1152라인 단일 파일로 진주 데이터(`jinjuBoundary`, `jinjuLegalBoundary`, `jinjuBusStops`, `jinjuTransitUsage`, `jinjuDistricts`)를 직접 import한다. `cityCode` prop을 받아 진주가 아니면 진주 데이터를 사용하지 않도록 분기한다(import는 그대로 유지해 트리 복잡도를 늘리지 않음).

**Files:**
- Modify: `src/components/Map/MapContainer.tsx`

- [ ] **Step 1: prop 시그니처와 초기 분기 진입점 추가**

`MapContainer.tsx` 컴포넌트 함수 위쪽에 다음을 적용한다.

기존 (대략 1500자 단위 첫 줄들):
```tsx
export default function MapContainer() {
  ...
```

변경 후:
```tsx
type MapContainerProps = {
  cityCode?: string;  // 행정표준코드 5자리. 미지정 시 진주(기존 동작).
};

const JINJU_CODE = "48170";

export default function MapContainer({ cityCode = JINJU_CODE }: MapContainerProps = {}) {
  const isJinju = cityCode === JINJU_CODE;
  ...
```

- [ ] **Step 2: 진주 데이터 사용처에 `isJinju` 가드 추가**

`MapContainer.tsx` 안에서 `jinjuBoundary`, `jinjuLegalBoundary`, `jinjuBusStops`, `jinjuTransitUsage`, `jinjuDistricts`, `jinjuUsageById`, `jinjuTransitUsage`(이용량 패널) 사용처에 모두 `if (!isJinju) return;` 또는 빈 배열/빈 객체로 대체하는 가드를 적용한다.

검색·확인 명령:
```bash
grep -n "jinju[A-Z]" src/components/Map/MapContainer.tsx
```

각 줄을 검사해서:
- 데이터 import 직접 사용 → `isJinju ? jinjuXxx : <빈값>`
- 부수 효과(useEffect 안에서 fetch·렌더) → 함수 본문 진입 직후 `if (!isJinju) return;`
- 토글 UI에서 "활성화" 조건 → `disabled={!isJinju}` + 라벨에 "준비 중" 추가

> 변경할 위치가 많아 한 번에 다 처리한다. step 단위로 쪼개기보다 한 번에 검색·치환 후 다음 step에서 실제 동작을 검증한다.

- [ ] **Step 3: dev 서버에서 진주 기본 동작 확인**

dev 서버 기동 후 `http://localhost:3000/map` (아직 라우트 살아있음) 접속해 기존 진주 화면이 그대로 보이는지 확인. 모든 레이어가 이전과 동일하게 동작해야 한다 (기본값이 진주이므로 회귀 0).

- [ ] **Step 4: 커밋**

```bash
git add src/components/Map/MapContainer.tsx
git commit -m "$(cat <<'EOF'
MapContainer에 cityCode prop 추가, 진주 외 도시에서 진주 데이터 비활성

cityCode 기본값은 진주(48170). 다른 도시에서는 jinjuBoundary 등
진주 전용 데이터·레이어를 비활성화. 토글 UI는 disabled + '준비 중'.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: `/[city]/page.tsx` 신설 — 도시별 메인

`src/app/[city]/page.tsx`를 만들어 도시 파라미터를 검증하고 `MapContainer`에 `cityCode`를 주입한다. 화면은 기존 `/map`(MapContainer 단독)과 동일하게 시작.

**Files:**
- Create: `src/app/[city]/page.tsx`

- [ ] **Step 1: 파일 작성**

```tsx
"use client";

import dynamic from "next/dynamic";
import { notFound, useParams } from "next/navigation";
import { cities } from "@/config/cities";

const MapContainer = dynamic(
  () => import("@/components/Map/MapContainer"),
  { ssr: false }
);

export default function CityPage() {
  const params = useParams<{ city: string }>();
  const cityKey = params.city;
  const city = cities[cityKey];

  if (!city) {
    notFound();
  }

  return (
    <main className="h-dvh w-full overflow-hidden">
      <MapContainer cityCode={city.code} />
    </main>
  );
}
```

- [ ] **Step 2: dev 서버에서 진주 라우트 동작 확인**

브라우저: `http://localhost:3000/jinju`

Expected: 기존 `/map`과 동일한 화면(진주 카카오맵 + 모든 레이어). MapContainer가 cityCode="48170"을 받아 isJinju=true로 동작.

- [ ] **Step 3: 다른 도시 진입 확인**

브라우저: `http://localhost:3000/tongyeong`

Expected: 카카오맵이 통영 중심으로 표시되거나, 진주 중심으로 표시되거나(현 MapContainer 동작에 따라 다름). 핵심은 **에러 없이 렌더되고, 진주 전용 데이터·레이어가 보이지 않음**.

> MapContainer 내부에서 도시 중심 좌표 설정은 아직 진주 하드코딩일 수 있다. 다음 step에서 보정.

- [ ] **Step 4: MapContainer 초기 중심·줌을 cityCode 기반으로 보정**

`MapContainer.tsx`에서 카카오맵 초기화 시 `cities` 매핑에서 도시 설정을 찾아 `center`/`zoom`을 사용:

```tsx
import { cities, DEFAULT_CITY } from "@/config/cities";

// 컴포넌트 안에서
const cityConfig = Object.values(cities).find(c => c.code === cityCode) ?? DEFAULT_CITY;
// 초기 center: cityConfig.center, level: toKakaoLevel(cityConfig.zoom)
```

기존 진주 하드코딩 좌표/줌이 있는 곳을 찾아 위 값으로 교체.

- [ ] **Step 5: 라우트 가드 검증**

브라우저: `http://localhost:3000/zzz-bogus-city`

Expected: 404 페이지.

- [ ] **Step 6: 커밋**

```bash
git add src/app/[city]/page.tsx src/components/Map/MapContainer.tsx
git commit -m "$(cat <<'EOF'
/[city] 동적 라우트 신설, 도시 중심 좌표·줌 cityCode 기반

src/app/[city]/page.tsx에서 cities 매핑으로 도시 검증 후
MapContainer에 cityCode 주입. MapContainer 초기 카카오맵 중심/줌을
도시 설정에서 가져오도록 변경.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: `/[city]/district/[id]/page.tsx`로 이동

기존 `src/app/district/[id]/page.tsx`를 도시 하위로 옮긴다. 진주 외 도시는 데이터가 없으므로 안내 화면.

**Files:**
- Move: `src/app/district/[id]/page.tsx` → `src/app/[city]/district/[id]/page.tsx`

- [ ] **Step 1: 디렉토리 생성·파일 이동**

```bash
mkdir -p src/app/[city]/district/[id]
git mv "src/app/district/[id]/page.tsx" "src/app/[city]/district/[id]/page.tsx"
rmdir src/app/district/\[id\] src/app/district 2>/dev/null || true
```

- [ ] **Step 2: 파일 상단에 도시 파라미터 가드 추가**

`src/app/[city]/district/[id]/page.tsx` 컴포넌트 함수 시그니처를 변경:

기존:
```tsx
export default function DistrictPage({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  const { id } = use(params);
```

변경 후:
```tsx
import { notFound } from "next/navigation";
import { cities } from "@/config/cities";

export default function DistrictPage({
  params,
}: {
  params: Promise<{ city: string; id: string }>;
}) {
  const { city: cityKey, id } = use(params);

  // 진주 외 도시는 선거구 데이터가 없음 → 안내 화면
  if (cityKey !== "jinju") {
    if (!cities[cityKey]) notFound();
    return (
      <main className="min-h-dvh flex items-center justify-center bg-gray-50">
        <div className="text-center">
          <h1 className="text-xl font-bold text-gray-800">선거구 데이터 준비 중</h1>
          <p className="text-sm text-gray-500 mt-2">
            {cities[cityKey].name}의 선거구 상세 정보는 아직 제공되지 않습니다.
          </p>
        </div>
      </main>
    );
  }
```

이후 기존 진주 데이터 로직은 그대로.

- [ ] **Step 3: 카드 링크 경로 수정**

`src/app/page.tsx`(아직 진주 카드 리스트)에서 `/district/${...}` 링크를 `/jinju/district/${...}`로 변경:

```tsx
href={`/jinju/district/${encodeURIComponent(b.name)}?type=${electionType}`}
```

> 이 파일은 Task 7에서 통째로 교체되지만, 그 사이 단계에서 깨지지 않도록 지금 수정.

- [ ] **Step 4: dev 서버에서 진주 선거구 상세 확인**

브라우저: `http://localhost:3000/jinju/district/가선거구?type=local` (실제 선거구 이름은 데이터에 따라)

Expected: 기존 `/district/가선거구`와 동일한 화면.

- [ ] **Step 5: 다른 도시 선거구 상세 확인**

브라우저: `http://localhost:3000/tongyeong/district/test`

Expected: "선거구 데이터 준비 중" 안내.

- [ ] **Step 6: 커밋**

```bash
git add src/app
git commit -m "$(cat <<'EOF'
선거구 상세를 /[city]/district/[id]로 이동

기존 /district/[id]는 제거되고 도시 네임스페이스 하위로 이동.
진주 외 도시는 '데이터 준비 중' 안내 화면.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: `GyeongnamMap` 컴포넌트 작성

`/` 라우트에서 사용할 경남 전역 지도 컴포넌트. 22개 시군구 폴리곤 + 인구 코로플레스 + 선거 유형 탭. 시군구 클릭 시 `/[city]`로 이동.

**Files:**
- Create: `src/components/Map/GyeongnamMap.tsx`

- [ ] **Step 1: 인구 색상 함수와 컴포넌트 골격**

```tsx
"use client";

import { useEffect, useRef, useState } from "react";
import { useRouter } from "next/navigation";
import { useKakaoMap, toKakaoLevel } from "@/hooks/useKakaoMap";
import { GYEONGNAM_VIEW } from "@/config/cities";
import gyeongnamCities from "@/data/gyeongnam-cities.json";

const ELECTION_TYPES = [
  { key: "local",       label: "기초의원" },
  { key: "provincial",  label: "도의원" },
  { key: "assembly",    label: "국회의원" },
  { key: "mayor",       label: "시장·군수" },
] as const;

function populationColor(p: number): string {
  if (p >= 500000) return "#7F1D1D";
  if (p >= 300000) return "#DC2626";
  if (p >= 150000) return "#F87171";
  if (p >= 80000)  return "#FCA5A5";
  if (p >= 40000)  return "#FECACA";
  return "#FEE2E2";
}

export default function GyeongnamMap() {
  const mapRef = useRef<HTMLDivElement>(null);
  const router = useRouter();
  const [electionType, setElectionType] = useState<string>("local");

  const { map, ready } = useKakaoMap(mapRef, {
    center: GYEONGNAM_VIEW.center,
    level: toKakaoLevel(GYEONGNAM_VIEW.zoom),
  });

  // 시군구 폴리곤 로드·표시
  useEffect(() => {
    if (!ready || !map) return;
    let cancelled = false;
    const polygons: any[] = [];

    fetch("/data/gyeongnam-boundary.json")
      .then(r => r.json())
      .then(geo => {
        if (cancelled) return;
        for (const feat of geo.features) {
          const cityKey = feat.properties.key as string;
          const cityName = feat.properties.name as string;
          const summary = (gyeongnamCities as any)[cityKey];
          const color = populationColor(summary?.totalPopulation ?? 0);

          for (const ring of feat.geometry.coordinates) {
            const path = ring[0].map(([lng, lat]: [number, number]) =>
              new window.kakao.maps.LatLng(lat, lng),
            );
            const polygon = new window.kakao.maps.Polygon({
              path,
              strokeWeight: 1,
              strokeColor: "#FFFFFF",
              strokeOpacity: 0.8,
              fillColor: color,
              fillOpacity: 0.6,
            });
            polygon.setMap(map);

            window.kakao.maps.event.addListener(polygon, "click", () => {
              router.push(`/${cityKey}`);
            });
            window.kakao.maps.event.addListener(polygon, "mouseover", () => {
              polygon.setOptions({ fillOpacity: 0.85 });
            });
            window.kakao.maps.event.addListener(polygon, "mouseout", () => {
              polygon.setOptions({ fillOpacity: 0.6 });
            });

            polygons.push(polygon);
          }
        }
      });

    return () => {
      cancelled = true;
      polygons.forEach(p => p.setMap(null));
    };
  }, [ready, map, router]);

  return (
    <div className="relative h-dvh w-full">
      <div ref={mapRef} className="absolute inset-0" />

      {/* 상단 탭 */}
      <div className="absolute top-4 left-1/2 -translate-x-1/2 z-10 flex gap-1 bg-white/95 backdrop-blur rounded-full shadow-md px-1 py-1">
        {ELECTION_TYPES.map(({ key, label }) => (
          <button
            key={key}
            onClick={() => setElectionType(key)}
            className={`text-sm px-3 py-1.5 rounded-full transition-colors ${
              electionType === key
                ? "bg-gray-900 text-white"
                : "text-gray-600 hover:bg-gray-100"
            }`}
          >
            {label}
          </button>
        ))}
      </div>

      {/* 좌측 안내 */}
      <div className="absolute top-4 left-4 z-10 bg-white/95 backdrop-blur rounded-lg shadow-md px-4 py-3 max-w-xs">
        <h1 className="text-base font-bold text-gray-900">경상남도 지방선거 분석</h1>
        <p className="text-xs text-gray-500 mt-1">시·군을 클릭하면 도시별 상세로 이동</p>
        <p className="text-[11px] text-gray-400 mt-1">
          현재 선거구 데이터는 진주시만 제공 (다른 도시 준비 중)
        </p>
      </div>

      {/* 우측 범례 */}
      <div className="absolute bottom-4 right-4 z-10 bg-white/95 backdrop-blur rounded-lg shadow-md px-3 py-2 text-xs">
        <div className="font-semibold text-gray-700 mb-1">인구</div>
        <div className="flex items-center gap-1">
          <span className="w-3 h-3 rounded" style={{ background: "#FEE2E2" }} /> ~4만
          <span className="w-3 h-3 rounded ml-2" style={{ background: "#FCA5A5" }} /> ~15만
          <span className="w-3 h-3 rounded ml-2" style={{ background: "#F87171" }} /> ~30만
          <span className="w-3 h-3 rounded ml-2" style={{ background: "#DC2626" }} /> 30만+
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 선거 유형 탭은 표시만 (다음 작업에서 데이터 연결)**

이번 단계에서는 탭 클릭 시 `electionType` state만 바뀌고 지도에는 변화 없음. 진주 선거구 데이터를 경남 지도 위에 표시하는 건 후속 작업 (스펙 9. 비범위에 가깝지만 진주 영역에서만 보이는 정도라면 적은 변경으로 가능 — 별도 task로 분리하지 않고 다음 Task에서 처리).

- [ ] **Step 3: 커밋**

```bash
git add src/components/Map/GyeongnamMap.tsx
git commit -m "$(cat <<'EOF'
GyeongnamMap 컴포넌트 추가

경남 22개 시군구 폴리곤을 인구로 코로플레스 색칠. 클릭 시 /[city]
이동. 선거 유형 탭은 UI만 (데이터 연결은 후속).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: 루트 `/`를 `GyeongnamMap`으로 교체

기존 진주 브리핑 카드 화면을 `GyeongnamMap`으로 교체. 브리핑 카드의 정보는 `/jinju`(MapContainer) 내부 패널에 이미 있으므로 소실되지 않는다.

**Files:**
- Modify: `src/app/page.tsx`

- [ ] **Step 1: page.tsx 전면 교체**

```tsx
"use client";

import dynamic from "next/dynamic";

const GyeongnamMap = dynamic(
  () => import("@/components/Map/GyeongnamMap"),
  { ssr: false }
);

export default function Home() {
  return <GyeongnamMap />;
}
```

- [ ] **Step 2: dev 서버에서 루트 확인**

브라우저: `http://localhost:3000/`

Expected:
- 카카오맵이 경남 전역(중심 [35.25, 128.45], 줌 9)으로 표시
- 22개 시군구 폴리곤이 인구로 색칠
- 진주(48170)는 인구 약 30만 → 진한 빨강 (#DC2626) 또는 그 이상
- 시군구 위에 마우스 올리면 fillOpacity 변화, 클릭 시 `/jinju` 등으로 이동
- 상단에 선거 유형 탭, 좌측 안내, 우측 범례

- [ ] **Step 3: 클릭 동선 검증**

`/`에서 진주 시군구 클릭 → `/jinju` 이동 → 진주 전체 레이어(인구·선거구·교통·경로당·상권·핀·사진) 정상.

`/`에서 통영 시군구 클릭 → `/tongyeong` 이동 → 카카오맵 통영 중심 표시, 진주 전용 레이어는 토글 disabled/"준비 중".

- [ ] **Step 4: 커밋**

```bash
git add src/app/page.tsx
git commit -m "$(cat <<'EOF'
루트 / 를 경남 전역 지도로 교체

기존 진주 브리핑 카드 리스트는 제거. 동일 정보는 /jinju MapContainer
사이드 패널에서 확인 가능. 클릭 동선: / → 시군구 클릭 → /[city].

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: 진주 선거구 폴리곤을 경남 지도에 표시

`GyeongnamMap`에 진주 행정동 폴리곤을 선거구별 색깔로 색칠해 표시. `electionType` 탭 변경 시 색깔 재계산.

**Files:**
- Modify: `src/components/Map/GyeongnamMap.tsx`

- [ ] **Step 1: 진주 선거구 매핑 헬퍼 추가**

`GyeongnamMap.tsx` 상단에 import 추가:

```tsx
import jinjuBoundary from "@/data/jinju-boundary.json";
import jinjuDistricts from "@/data/jinju-districts.json";
```

컴포넌트 함수 밖에 색상 팔레트와 매핑 함수:

```tsx
const DISTRICT_COLORS = [
  "#2563EB", "#DC2626", "#16A34A", "#CA8A04",
  "#9333EA", "#EA580C", "#0891B2", "#BE185D",
];

function jinjuDongDistrictMap(electionType: string): Record<string, { name: string; color: string }> {
  const types = (jinjuDistricts as any).types ?? {};
  const districts = types[electionType]?.districts ?? [];
  const result: Record<string, { name: string; color: string }> = {};
  districts.forEach((d: any, idx: number) => {
    const color = DISTRICT_COLORS[idx % DISTRICT_COLORS.length];
    (d.dongs || []).forEach((dong: string) => {
      result[dong] = { name: d.name, color };
    });
  });
  return result;
}
```

- [ ] **Step 2: 진주 동 폴리곤 렌더링 useEffect 추가**

`GyeongnamMap` 컴포넌트 안에 시군구 폴리곤 useEffect 아래에 추가:

```tsx
// 진주 선거구 레이어: 행정동 폴리곤을 선거구별 색깔로 색칠
useEffect(() => {
  if (!ready || !map) return;
  const mapping = jinjuDongDistrictMap(electionType);
  const polygons: any[] = [];

  for (const feat of (jinjuBoundary as any).features) {
    const dongName = feat.properties.name;
    const entry = mapping[dongName];
    if (!entry) continue;

    const geom = feat.geometry;
    const rings = geom.type === "Polygon" ? [geom.coordinates] : geom.coordinates;

    for (const polygon of rings) {
      const path = polygon[0].map(([lng, lat]: [number, number]) =>
        new window.kakao.maps.LatLng(lat, lng),
      );
      const p = new window.kakao.maps.Polygon({
        path,
        strokeWeight: 2,
        strokeColor: entry.color,
        strokeOpacity: 1.0,
        fillColor: entry.color,
        fillOpacity: 0.35,
        zIndex: 5,  // 시군구 폴리곤 위에 표시
      });
      p.setMap(map);
      polygons.push(p);
    }
  }

  return () => {
    polygons.forEach(p => p.setMap(null));
  };
}, [ready, map, electionType]);
```

- [ ] **Step 3: 좌측 안내 문구 수정**

기존 안내에서 "선거구 데이터는 진주시만 제공" 문구는 그대로 두되, 선거 유형 탭 동작이 가시화되었음을 반영해 안내 보강:

```tsx
<p className="text-[11px] text-gray-400 mt-1">
  진주시는 선거 유형 탭에 따라 선거구별 색깔 표시 (다른 시·군 준비 중)
</p>
```

- [ ] **Step 4: dev 서버에서 검증**

브라우저: `http://localhost:3000/`

Expected:
- 22개 시군구 코로플레스 위에 진주 행정동이 선거구별 색깔(예: local일 때 가/나/다/라 4개 색)로 덧칠
- 상단 탭 클릭(기초/도/국/시장) → 진주 영역 색깔이 즉시 바뀜
- mayor 탭: 진주 전체가 단일 색

- [ ] **Step 5: 커밋**

```bash
git add src/components/Map/GyeongnamMap.tsx
git commit -m "$(cat <<'EOF'
진주 선거구 폴리곤을 경남 지도에 오버레이

GyeongnamMap에 진주 행정동을 선거 유형별 선거구 색깔로 색칠.
electionType 탭 변경 시 색깔 재계산. 다른 시군구는 데이터 없음.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: 구 라우트 `/map` 삭제

`/jinju`가 같은 역할을 하므로 `/map`은 불필요.

**Files:**
- Delete: `src/app/map/page.tsx`

- [ ] **Step 1: 파일 삭제**

```bash
git rm src/app/map/page.tsx
rmdir src/app/map 2>/dev/null || true
```

- [ ] **Step 2: 참조 검색**

```bash
grep -rn "/map" src/ --include="*.tsx" --include="*.ts" | grep -v "kakao\.maps\|api/map\|/map/page" | head
```

`href="/map"` 같은 링크가 남아 있으면 `/jinju`로 교체. 기존 진주 브리핑 페이지(page.tsx)에 있던 `href="/map"` 링크는 이미 Task 7에서 page.tsx가 통째로 교체되어 사라졌으므로 추가 작업 없음. 다른 곳에서 참조가 있으면 찾아서 수정.

- [ ] **Step 3: dev 서버에서 `/map` 접속 시 404 확인**

브라우저: `http://localhost:3000/map`

Expected: 404.

- [ ] **Step 4: 커밋**

```bash
git add -A
git commit -m "$(cat <<'EOF'
구 라우트 /map 제거

/jinju가 같은 역할(MapContainer + 진주 데이터)을 수행하므로 불필요.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: 전체 동선 회귀 점검

데이터·라우트·UI가 함께 작동하는지 최종 확인. 코드 변경 없이 검증만.

- [ ] **Step 1: 빌드 통과 확인**

```bash
PATH="$PWD/node_modules/.bin:$PATH" next build
```

Expected: 에러 없이 빌드 성공. 라우트 목록에 `/`, `/[city]`, `/[city]/district/[id]` 포함.

- [ ] **Step 2: dev 서버 기동**

```bash
PATH="$PWD/node_modules/.bin:$PATH" next dev
```

(터미널에서 직접 실행 — 별도 셸 환경 필요)

- [ ] **Step 3: 동선 체크리스트**

브라우저로 다음을 확인:

| 동선 | 기대 결과 |
|---|---|
| `/` | 경남 전역 지도, 22개 시군구 색칠, 상단 선거 탭, 좌측 안내, 우측 범례 |
| `/` → 진주 클릭 | `/jinju`로 이동, 진주 전체 레이어 활성 |
| `/jinju` 직접 접속 | 진주 카카오맵 + 모든 레이어(인구·선거구·교통·경로당·상권·핀·사진) |
| `/jinju/district/가선거구?type=local` | 진주 가선거구 상세 |
| `/` → 통영 클릭 | `/tongyeong`으로 이동, 카카오맵 통영 중심 |
| `/tongyeong` | 진주 전용 레이어 모두 비활성/"준비 중" 배지 |
| `/tongyeong/district/test` | "선거구 데이터 준비 중" 안내 |
| `/zzz` | 404 |
| `/map` | 404 |
| `/district/가선거구` | 404 |

- [ ] **Step 4: 콘솔 에러 확인**

브라우저 개발자 도구 콘솔에서 에러·경고 확인. 카카오맵 관련 경고는 무시 가능.

- [ ] **Step 5: 회귀 점검 결과 정리**

발견된 이슈가 있으면 항목별로 fixup 커밋. 없으면 다음 step.

- [ ] **Step 6: 최종 푸시**

```bash
git push origin main
```

> 이 프로젝트는 PR 없이 main에 직접 푸시(memory: `feedback_direct_push.md`).

---

## Self-Review 결과

- **스펙 커버리지:** 스펙 §3 라우트 구조 → Task 4·5·8 / §4 데이터 흐름 → Task 1·2 / §5 컴포넌트 → Task 3·6 / §6 빈 상태 → Task 3·5 / §7 미커밋 처리 → Task 0 / §8 단계 → 전 Task. ✅
- **플레이스홀더:** 없음. 모든 코드 블록 실제 내용 포함.
- **타입 일관성:** `cityCode` (string, 행정표준코드 5자리), `cityKey` (string, URL 세그먼트) — 모든 Task에서 동일 명명 사용. `MapContainerProps`는 Task 3에서 정의, Task 4에서 사용.
- **선거 유형 탭 데이터 연결:** Task 6에서 UI만, 진주 선거구 폴리곤을 경남 지도에 덧씌우는 작업은 스펙 §3에 "선거구 레이어는 진주 영역에서만 폴리곤이 그려지고…"로 포함되어 있는데 이번 plan에서는 후속으로 미뤘다. 사용자 검토 시 확인 필요.

---

## 비범위

- MapContainer 내부 리팩토링·컴포넌트 분할(1152라인 그대로)
- 진주 외 시군구의 교통·경로당·상권·선거구 데이터 수집
- DB 도입
- 다른 광역시도 확장
- `/` 지도 위에 진주 선거구 폴리곤 오버레이 (선거 유형 탭 데이터 연결)
- 카드 리스트 화면(기존 `/`) 별도 보존 — 사이드 패널에 같은 정보가 있으므로 삭제. 필요 시 후속에서 `/[city]/briefing` 신설.
